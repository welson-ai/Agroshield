import { TestSpinning } from '@/components/test-spinning'

// Test spinning page component for transaction testing
export default function TestSpinningPage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-red-50 via-orange-50 to-amber-50 p-4">
      <TestSpinning />
    </div>
  )
}
